# 🔄 모델 파이프라인 전체 구조 설명

## 📊 전체 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                    데이터 파이프라인 (Daily)                      │
│  CSV → DB 백필 → Historical Data 준비                            │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│              모델 학습 파이프라인 (Weekly)                        │
│  Trend + Fourier (Grid Search) + LSTM (Random Search)            │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│              모델 추론 파이프라인 (Daily)                         │
│  3단계 하이브리드 예측: Trend + Seasonality + Residual           │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│                    예측 결과 저장                                 │
│  PostgreSQL → FastAPI → 사용자                                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 1️⃣ 데이터 파이프라인 (Daily Backfill)

### 📅 스케줄: 매일 1:00 AM UTC

### 🔄 프로세스:

```
CSV 파일 (power_demand_final.csv)
    ↓
[extract_daily_slice]
    ├─ 전날(yesterday) 24시간 데이터 추출
    └─ 타임스탬프, 수요값만 선택
    ↓
[load_raw_demand]
    ├─ 기존 같은 날짜 데이터 삭제 (중복 방지)
    ├─ PostgreSQL raw_demand 테이블에 삽입
    └─ data_load_state 테이블에 상태 기록
    ↓
✅ 완료: DB에 최신 24시간 데이터 추가
```

### 📁 관련 파일:
- `flows/data_ingestion.py`: `daily_data_ingestion_flow`

### 📊 데이터 구조:
- **입력**: CSV 파일 (`일시`, `power demand(MW)`)
- **출력**: PostgreSQL `raw_demand` 테이블
  - `timestamp`: 시간
  - `demand_value`: 전력 수요 (MW)
  - `location`: 위치
  - `source`: 데이터 출처

---

## 2️⃣ 모델 학습 파이프라인 (Weekly Retrain)

### 📅 스케줄: 매주 일요일 3:00 AM UTC

### 🎯 목적: 3단계 하이브리드 모델 재학습 및 하이퍼파라미터 최적화

### 🔄 전체 프로세스:

```
[1단계] 데이터 로드 및 분할
    ├─ CSV에서 전체 데이터 로드
    ├─ Train/Val/Test 분할
    └─ Train: ~2024-08-31, Val: ~2024-09-30, Test: 이후
    ↓
[2단계] Trend Model 학습 (Log-Linear Regression)
    ├─ log(demand) = α + β*t
    ├─ OLS 회귀 분석
    └─ 추세 성분 추출
    ↓
[3단계] Fourier Seasonality Model 학습 (Grid Search)
    ├─ Detrended 데이터에 대해
    ├─ Grid Search: Kd, Kw, Ky 각각 1~14 탐색
    ├─ 총 2,744개 조합 (14×14×14)
    ├─ Validation MSE 기준 최적 조합 선택
    └─ 계절성 성분 추출
    ↓
[4단계] Residual 계산
    ├─ Residual = Detrended - Seasonality
    └─ LSTM 학습용 데이터 준비
    ↓
[5단계] LSTM Residual Model 학습 (Random Search)
    ├─ 50회 Random Search iteration
    ├─ 13개 하이퍼파라미터 탐색
    ├─ 각 trial: 최대 100 epochs (Early stopping)
    ├─ Validation loss 기준 최적 모델 선택
    └─ 잔차 예측 모델 완성
    ↓
[6단계] 모델 저장
    ├─ trend_model.pkl
    ├─ fourier_model.pkl (최적 Kd, Kw, Ky 포함)
    ├─ lstm_model.pth (최적 하이퍼파라미터 포함)
    ├─ residual_scaler.pkl
    └─ config.json (모든 설정 저장)
    ↓
[7단계] Test Set 평가
    ├─ Rolling forecast 평가
    ├─ MAE, RMSE, MAPE, SMAPE 계산
    └─ MLflow에 메트릭 로깅
    ↓
✅ 완료: Production 모델 업데이트
```

### 📈 3단계 모델 상세:

#### **Stage 1: Trend Model (Log-Linear Regression)**
```python
# 수식
log(demand_t) = α + β * t

# 학습
- 입력: 시간 인덱스 t
- 출력: log(demand) 예측
- 변환: exp(log_pred) → trend 예측값
- 목적: 장기 추세 포착
```

#### **Stage 2: Fourier Seasonality Model (Grid Search)**
```python
# Fourier Series
seasonality = Σ [a_k * sin(2πkt/P) + b_k * cos(2πkt/P)]

# 주기
- Daily: P = 24시간, Kd = 1~14
- Weekly: P = 168시간, Kw = 1~14  
- Yearly: P = 8766시간, Ky = 1~14

# Grid Search
- 탐색 범위: Kd × Kw × Ky = 2,744개 조합
- 평가 기준: Validation MSE
- 최적 조합 선택 후 Linear Regression 학습
```

#### **Stage 3: LSTM Residual Model (Random Search)**
```python
# 모델 구조
Seq2Seq LSTM (Univariate)
- Input: 168시간 (7일) residual history
- Output: 24시간 residual 예측

# 하이퍼파라미터 탐색 (Random Search, 50회)
- hidden_size: [64, 128, 256]
- num_layers: [2, 3]
- dropout: [0.1, 0.2, 0.3]
- bidirectional: [False, True]
- use_attention: [False, True]
- batch_size: [32, 64]
- learning_rate: [0.0005, 0.001, 0.005]
- optimizer: ['adamw']
- weight_decay: [0.0, 1e-4]
- grad_clip: [0.5, 1.0]
- teacher_forcing_ratio: [0.0, 0.3, 0.5, 0.7]
- scheduler: ['cosine', 'reduce']
- early_stopping_patience: [10, 15]

# 평가 기준: Validation Loss (MSE)
```

### 📁 관련 파일:
- `flows/weekly_retrain_hybrid.py`: `weekly_retrain_flow`
- `src/models/train_pipeline.py`: `PowerDemandTrainer`

---

## 3️⃣ 모델 추론 파이프라인 (Daily Forecast)

### 📅 스케줄: 매일 2:00 AM UTC (백필 후)

### 🎯 목적: 다음 24시간 전력 수요 예측

### 🔄 전체 프로세스:

```
[1단계] Historical Data 로드
    ├─ DB에서 최근 168시간 (7일) 데이터 조회
    └─ CSV fallback (DB에 데이터 부족 시)
    ↓
[2단계] 모델 로드
    ├─ trend_model.pkl
    ├─ fourier_model.pkl (최적 Kd, Kw, Ky)
    ├─ lstm_model.pth (최적 하이퍼파라미터)
    ├─ residual_scaler.pkl
    └─ config.json
    ↓
[3단계] Historical Context 준비
    ├─ Trend 성분 계산 (historical)
    ├─ Detrended = Demand - Trend
    ├─ Seasonality 성분 계산 (historical)
    └─ Residual = Detrended - Seasonality
    ↓
[4단계] 3단계 예측 실행
    ├─ [4-1] Trend 예측
    │   └─ log-linear extrapolation (24시간)
    │
    ├─ [4-2] Seasonality 예측
    │   ├─ Fourier terms 생성 (미래 24시간)
    │   ├─ Exogenous features 생성 (시간, 요일, 계절 등)
    │   └─ Fourier model 예측
    │
    └─ [4-3] Residual 예측 (LSTM)
        ├─ 최근 168시간 residual → LSTM 입력
        └─ LSTM 예측 (24시간 residual)
    ↓
[5단계] 최종 예측 결합
    ├─ Final Forecast = Trend + Seasonality + Residual
    └─ 24시간 예측값 생성
    ↓
[6단계] DB 저장
    ├─ 기존 예측 삭제 (같은 시간대)
    ├─ predictions 테이블에 저장
    └─ 각 성분별로 저장 (trend, seasonal, lstm)
    ↓
✅ 완료: 다음 24시간 예측 완료
```

### 📊 예측 수식:

```
Forecast(t) = Trend(t) + Seasonality(t) + Residual(t)

where:
  Trend(t) = exp(α + β * t)                    # Log-linear extrapolation
  Seasonality(t) = Fourier(Kd, Kw, Ky, t)      # Fourier series
  Residual(t) = LSTM(residual_history)         # Seq2Seq LSTM
```

### 📁 관련 파일:
- `flows/daily_forecast.py`: `daily_forecast_flow`
- `src/models/inference_pipeline.py`: `PowerDemandForecaster`

---

## 4️⃣ 데이터 흐름 다이어그램

```
┌─────────────────────────────────────────────────────────────┐
│                    DAILY OPERATION                           │
└─────────────────────────────────────────────────────────────┘

01:00 UTC ──→ [Daily Backfill]
                CSV → PostgreSQL (raw_demand)
                └─ 24시간 데이터 추가

02:00 UTC ──→ [Daily Forecast]
                Historical (168h) → 3-Stage Model → Predictions (24h)
                └─ PostgreSQL (predictions) 저장

───────────────────────────────────────────────────────────────

┌─────────────────────────────────────────────────────────────┐
│                    WEEKLY OPERATION                          │
└─────────────────────────────────────────────────────────────┘

일요일 03:00 UTC ──→ [Weekly Retrain]
                        ├─ Trend Model 학습
                        ├─ Fourier Grid Search (2,744 조합)
                        ├─ LSTM Random Search (50 iterations)
                        └─ Production 모델 업데이트
```

---

## 5️⃣ 모델 저장 구조

```
models/production/
├── config.json              # 모델 설정 (window_size, horizon, 하이퍼파라미터)
├── trend_model.pkl          # Log-linear regression 모델
├── fourier_model.pkl        # Fourier model + 최적 Kd, Kw, Ky
├── lstm_model.pth           # LSTM checkpoint + 최적 하이퍼파라미터
└── residual_scaler.pkl      # Residual scaling (StandardScaler)
```

### config.json 예시:
```json
{
  "window_size": 168,
  "horizon": 24,
  "fourier_params": {
    "Kd": 5,    # Grid Search로 찾은 최적값
    "Kw": 8,    # Grid Search로 찾은 최적값
    "Ky": 2     # Grid Search로 찾은 최적값
  },
  "lstm_params": {
    "hidden_size": 256,
    "num_layers": 3,
    "dropout": 0.3,
    "bidirectional": false,
    "use_attention": false,
    "learning_rate": 0.001,
    "optimizer": "adamw",
    ...
  },
  "timestamp": "2025-11-20T17:47:27.354364"
}
```

---

## 6️⃣ 주요 특징

### ✅ 장점:
1. **3단계 분해**: Trend, Seasonality, Residual을 각각 모델링하여 해석 가능성 향상
2. **자동 하이퍼파라미터 최적화**: 
   - Fourier: Grid Search (2,744 조합)
   - LSTM: Random Search (50 iterations)
3. **일일 자동 업데이트**: 매일 새로운 데이터로 예측 갱신
4. **주간 모델 개선**: 매주 새로운 데이터로 모델 재학습

### 📊 성능 지표:
- **MAE** (Mean Absolute Error)
- **RMSE** (Root Mean Squared Error)
- **MAPE** (Mean Absolute Percentage Error)
- **SMAPE** (Symmetric MAPE)

### 🔄 자동화:
- Prefect로 스케줄 관리
- MLflow로 실험 추적
- Grafana로 모니터링
- PostgreSQL로 데이터 저장

---

## 7️⃣ 실행 흐름 요약

### Daily (매일):
1. **01:00 UTC**: CSV → DB 백필 (24시간)
2. **02:00 UTC**: Historical → 3-Stage Model → 예측 (24시간)

### Weekly (일요일):
1. **03:00 UTC**: 전체 데이터로 모델 재학습
   - Trend 학습
   - Fourier Grid Search (2,744 조합)
   - LSTM Random Search (50회)
   - Test Set 평가
   - Production 배포

---

## 📝 요약

**전체 파이프라인은 3단계 하이브리드 모델을 사용합니다:**

1. **Trend**: Log-linear regression으로 장기 추세 포착
2. **Seasonality**: Fourier series (Grid Search로 최적화)로 주기 패턴 포착
3. **Residual**: Seq2Seq LSTM (Random Search로 최적화)로 비선형 패턴 포착

**최종 예측 = Trend + Seasonality + Residual**

매일 자동으로 예측이 생성되고, 매주 모델이 재학습되어 성능이 지속적으로 개선됩니다!